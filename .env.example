# Disposable-email check, server-side only (src/lib/email-domain-check.server.ts).
# Never prefix these with NEXT_PUBLIC_: they must not reach the client bundle.
MAILCHECK_API_URL=
MAILCHECK_API_KEY=
